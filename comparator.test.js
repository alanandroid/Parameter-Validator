/**
 * Tests. What did you expect?
 */
import {
  definedComparator,
  notNullComparator,
  truthyComparator } from './comparator';

describe('tests for allParametersDefined', () => {
  beforeEach(() => {
    //do something
  })
  afterAll(() => {
    //do something
  })
  test(expect(true).toBe(true))
});
